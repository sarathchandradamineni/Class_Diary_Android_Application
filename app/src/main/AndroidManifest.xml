<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sarath.cseiimdpjntuk">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Main2Activity"
            android:label="@string/title_activity_main2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Details" />
        <activity android:name=".share_image" />
        <activity android:name=".CLC_FILES" />
        <activity android:name=".ACN_FILES" />
        <activity android:name=".MS_FILES" />
        <activity android:name=".UML_FILES" />
        <activity android:name=".UMLLAB_FILES" />
        <activity android:name=".alerts"></activity>
    </application>

</manifest>